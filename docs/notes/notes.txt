UNOPSIS (TEAM 5): PROJECT NOTES
================================
Weekly progress, notes for teammates, reminders, clarifications and challenges.
Newest week at the bottom of each section. Keep adding an entry every week.

Lines marked [TEAM: ...] are for a teammate to fill in with real information.


1. TEAM
-------
Member 1: [TEAM: full name]   netid: [TEAM]   GitHub: Revaant-D   (owns the repository)
Member 2: [TEAM: full name]   netid: [TEAM]   GitHub: [TEAM]
Member 3: [TEAM: full name]   netid: [TEAM]   GitHub: [TEAM]
Member 4: [TEAM: full name]   netid: [TEAM]   GitHub: [TEAM]

Project: Unopsis (Django project "inboxtriage", app "unopsis").
Repository: 5_Unopsis. Instructor collaborator: 27guptamohit.


2. WEEK 3 UPDATE (team level, updated 2026-09-21)
-------------------------------------------------
Assignment: repository setup, split settings, secrets, GitHub workflow, four kinds of views, templates.

Done:
- Section 1: settings split into inboxtriage/settings/{base,development,production}.py
  (DEBUG=True only in development, DEBUG=False in production, ALLOWED_HOSTS set in both).
  SECRET_KEY and the dummy API key live in .env (git-ignored); .env.example is committed.
  .gitignore excludes .env, local databases and caches.
- GitHub workflow: work done on feature branches (split-settings, env-secrets, views, templates,
  docs) and merged into main with merge commits. See docs/branching_strategy/.
- Section 2: four kinds of views on the BriefItem model (see VIEW INVENTORY below).
- Section 3: base.html, one shared list template with for/empty, and a detail template.
- Tests: 7 tests (python manage.py test) cover the views, ordering, empty state and detail page.
- Screenshots of every view and of the empty state: docs/screenshots/.
- Wireframes: an error-state screen (a connected account such as Slack stops working) was added as
  the last page of docs/wireframes/v1/Wireframe.pdf and as error_state.png.

Still to do before submitting: see REMINDERS.


3. WEEK 3 UPDATE (per person)
-----------------------------
Each teammate: fill in your own block in your own words. Two or three lines each is enough.
(Examples of what to write: "reviewed the four views and ran the tests", "wrote the notes and the
reflection", "ran the project from a fresh clone on my machine", "checked the wireframes".)

Member 1: [TEAM: name]
  What I did this week:        [TEAM]
  What I will do next:         [TEAM]
  Blocked on / need help with: [TEAM: or write "nothing"]

Member 2: [TEAM: name]
  What I did this week:        [TEAM]
  What I will do next:         [TEAM]
  Blocked on / need help with: [TEAM: or write "nothing"]

Member 3: [TEAM: name]
  What I did this week:        [TEAM]
  What I will do next:         [TEAM]
  Blocked on / need help with: [TEAM: or write "nothing"]

Member 4: [TEAM: name]
  What I did this week:        [TEAM]
  What I will do next:         [TEAM]
  Blocked on / need help with: [TEAM: or write "nothing"]

Suggested split for the remaining work (edit freely):
- Repository owner: invite the instructor, make the repo public, submit the link on Canvas.
- Teammate B: click through all four views, the empty state and the detail page; run the tests.
- Teammate C: fill in this file and write the reflection; check the wireframes.
- Teammate D: run the project from a fresh clone on their own machine and report anything that fails.


4. NOTES FOR TEAMMATES
----------------------
Getting the project running (first time):
  git clone https://github.com/Revaant-D/5_Unopsis.git
  cd 5_Unopsis
  python -m venv .venv
  .venv\Scripts\activate                 (Mac/Linux: source .venv/bin/activate)
  pip install -r requirements.txt
  copy .env.example .env                 (Mac/Linux: cp .env.example .env)
  python -c "import secrets; print(secrets.token_urlsafe(50))"   then paste the result as SECRET_KEY in .env
  python manage.py migrate
  python manage.py createsuperuser       (use the username mohitg2, the demo data is owned by it)
  python manage.py seed_demo
  python manage.py runserver
Use "python", not "py", once the virtual environment is active. "py" can pick a different Python that
does not have this project's packages installed.

Rules for working together:
- Never commit .env. Run "git status" before every commit and check nothing secret is staged.
- Work on your own branch (feature/<topic> or docs/<topic>), commit in small steps, then merge into
  main. Prefer a Pull Request that another teammate has looked at. See docs/branching_strategy/.
- Commit under your own GitHub account so the history shows who did what.
- Run "python manage.py test" before merging into main.
- Do not rewrite shared history (no force-push) once teammates have cloned the repo.
- Update this file every week.


5. REMINDERS
------------
Before submitting:
  [ ] Instructor 27guptamohit invited as a collaborator (Settings > Collaborators) and has accepted
  [ ] Repository is public (open the link in an incognito window to check)
  [ ] Every [TEAM: ...] line in this file is filled in or removed
  [ ] Reflection (bonus) written in the team's own words
  [ ] Each teammate has run the project from a fresh clone at least once
  [ ] Link submitted on Canvas (only ONE teammate submits for the whole team)
Due date: [TEAM: date and time]


6. CLARIFICATIONS AND DECISIONS
-------------------------------
- Repository name: 5_Unopsis (team number + project name).
- The four views all use the BriefItem model, so they can share one list template.
- Lane order: BriefItem.lane is text, so the default ordering would list FYI first. The views sort
  Needs you, then Moving, then FYI, so the database schema was left unchanged. [TEAM: confirm]
- db.sqlite3 is not committed (it is data, not code). Teammates rebuild it with migrate and seed_demo.
- manage.py runs the development settings by default; wsgi.py and asgi.py default to production.
  Run production locally with: python manage.py runserver --settings=inboxtriage.settings.production
- Screenshots are stored in docs/screenshots/ and shown in the README.
- The wireframe error state shows an account (Slack) that stops working: the brief still opens with
  the sources that work, says what is missing, and offers one way to fix it.


7. CHALLENGES AND LESSONS SO FAR
--------------------------------
- Moving settings.py one folder deeper broke BASE_DIR; it needed one more .parent.
- The original settings.py had a hard-coded SECRET_KEY. A new key was generated and the old one
  was kept out of the Git history.
- BriefItem.lane is text, so Meta.ordering = ["lane", "rank"] sorts alphabetically: "fyi" first and
  "needs_you" last, the opposite of what the product wants. The views sort the lanes explicitly.
- Django still runs template tags that appear inside HTML comments, so a comment mentioning
  {% block title %} broke the page. Use {% comment %}...{% endcomment %} or {# ... #} instead.
- With DEBUG=False the local development server does not serve static files, so the admin looks
  unstyled in production mode. That is expected, not a bug.
- Running "py manage.py runserver" failed with "No module named 'environ'" because "py" used a Python
  without this project's packages. Activate the project's .venv and use "python".

Known issue for teammates:
- Running "python manage.py seed_demo" a second time adds duplicate briefs and items (the lookup
  includes the current time). To reset cleanly: delete db.sqlite3, then run migrate,
  createsuperuser (username mohitg2) and seed_demo once.

Open problems: [TEAM: anything the team is stuck on]


8. MEETING NOTES
----------------
[TEAM: date, who attended, what was decided, who does what next. Copy this block for each meeting.]
  Date:
  Attendees:
  Decisions:
  Action items (who, what, by when):


9. HOW TO RUN AND WHAT TO OPEN
------------------------------
  python manage.py runserver                     (development, DEBUG=True)
  python manage.py runserver --settings=inboxtriage.settings.production   (production, DEBUG=False)
  python manage.py test                          (7 tests for the views and templates)

  /items/manual/         view 1
  /items/render/         view 2
  /items/cbv-base/       view 3
  /items/cbv-generic/    view 4 (list)
  /items/<id>/           view 4 (detail), e.g. /items/1/
  Empty state: add ?state=snoozed to any list URL, e.g. /items/cbv-generic/?state=snoozed


10. VIEW INVENTORY (name, type, URL, purpose)
---------------------------------------------
All views live in unopsis/views.py, routes in unopsis/urls.py. Every route has a name=.

1. item_list_manual
   Type:    function-based view, HttpResponse (manual)
   URL:     /items/manual/        name: item-list-manual
   Purpose: lists every BriefItem. Loads the template with loader.get_template(), renders it with
            the context, and wraps the result in HttpResponse by hand, so each step is visible.

2. item_list_render
   Type:    function-based view, render() shortcut
   URL:     /items/render/        name: item-list-render
   Purpose: same page as view 1, but render(request, template, context) does the load, fill and
            wrap steps in one call.

3. ItemListBaseView
   Type:    class-based view, inherits from django.views.View
   URL:     /items/cbv-base/      name: item-list-cbv-base
   Purpose: same page again. get() queries BriefItem itself and returns render(...). The base
            View does not know about the model, so the query is written by hand.

4. ItemListView and ItemDetailView
   Type:    class-based generic views (ListView, DetailView), model = BriefItem
   URL:     /items/cbv-generic/   name: item-list-cbv-generic
            /items/<int:pk>/      name: item-detail
   Purpose: ListView shows the same list using the default template name
            (unopsis/briefitem_list.html) with context_object_name = "items". DetailView opens one
            item and its "receipts" (the messages it was built from).

Templates: templates/base.html (blocks: title, content), templates/unopsis/briefitem_list.html
(shared by all four list views; for/empty), templates/unopsis/briefitem_detail.html.


11. REFLECTION (optional bonus)
-------------------------------
[TEAM: rewrite this in your own words from your own experience. The points below are a starting
point drawn from what was built; delete or change anything that does not match what you felt.]

HttpResponse vs render:
- HttpResponse (manual) was useful for seeing what Django really does: find the template, fill it
  with data, wrap it as a response. It is fine for plain text or tiny responses.
- render() was what we would use for every real page. It is the same three steps in one line, so
  the view stays short and readable.

Base CBV vs Generic CBV:
- The base View gave full control: we wrote the query and chose the template ourselves. That is
  useful when the page does not fit a standard pattern.
- ListView/DetailView needed almost no code because "model = BriefItem" is enough for Django to
  fetch rows and find the template. That is best for standard list and detail pages, and we only
  overrode get_queryset() to keep the Needs-you-first ordering.
